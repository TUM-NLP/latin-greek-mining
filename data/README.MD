# Data

This is the open source version of the benchmark presented in the paper.
Results differ a bit, but not too much.
It consists of a train and test section, with corresponding gold sets.
Use these files with `contextual_sentence_embeddings.py`
to create embeddings with your specified model.